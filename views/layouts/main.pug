html
    head
        meta(charset='utf-8')
        title #{title[0].toUpperCase() + title.slice(1)} | ReactWarriors
        meta(name='description' content='The HTML5 Herald')
        meta(name='author' content='ReactWarriors team')
        link(rel='stylesheet' href='styles/main.css')
    body
        .app
            block content
        block foot
            #footer
                p some footer content
